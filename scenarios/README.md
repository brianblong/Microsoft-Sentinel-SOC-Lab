# Authorized Validation Scenarios

Scenarios are defensive validation exercises for isolated lab assets. They are not instructions for testing third-party systems.

Use `template.yml` as the starting point. Scenario definitions should separate analyst-visible information from protected ground truth.

Lifecycle:

```text
planned -> reviewed -> safe-to-run -> validated -> retired
```

